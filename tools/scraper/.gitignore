# The crawl pool is a local artifact — tens of MB and machine-specific.
# What ships is what export.py writes into data/catalog.json.
catalog.db
catalog.db-shm
catalog.db-wal
harvest.log

# these modules import each other, so running any of them leaves bytecode here
__pycache__/
*.pyc

# the image-check cache - a local artifact, rebuilt on demand
imgcheck.json
